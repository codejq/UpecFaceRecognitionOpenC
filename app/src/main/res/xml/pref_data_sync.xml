<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

    <!-- This preference simply launches an intent when selected. Use this UI sparingly, per
         design guidelines. -->

    <EditTextPreference
        android:defaultValue="Default value"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Gmail Account "
        android:key="gmail_account"
        android:summary="Gmail Account to send notification email from " />
    <EditTextPreference
        android:defaultValue="Default value"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Gmail Password"
        android:key="gmail_password"
        android:summary="Gmail Password of the account you want  to use for sending emails"
        android:password="true" />
    <EditTextPreference
        android:defaultValue="Default value"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Notifi mail  "
        android:key="notifi_email"
        android:summary="Enter email address which will recive and alert when an un authorized login attempted detected " />
</PreferenceScreen>
